import React from 'react'

const NotFound = () => {
    return (
        <div>

            <h1>Siden som dukker op, hvis du har valgt en route, som ikke findes!</h1>
            
        </div>
    )
}

export default NotFound

